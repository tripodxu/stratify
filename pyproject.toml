[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "stratify"
version = "0.1.0"
description = "Multi-agent class society simulation"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]